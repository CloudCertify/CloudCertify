# Base URL of the API the web app talks to.
# Unset = production (https://api-cloudcertify.snowye.dev), so no setup is needed.
# Copy this file to .env.local and uncomment to develop against a local API
# (`dotnet run --project API` serves on :8080).
# VITE_API_URL=http://localhost:8080
